# GitHub OAuth (for sign-in with GitHub)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# NextAuth
AUTH_SECRET=  # Generate with: openssl rand -base64 32
AUTH_URL=https://spectivity.io

# GitHub API (for sync script, optional but recommended)
GITHUB_TOKEN=
